<div class="card-body">
    <table id="example2" class="table table-bordered table-hover">
      <thead>
      <tr>
        <th>No</th>
        <th>Nama</th>
        <th>Aksi</th>
      </tr>
      </thead>
      <tbody>
        <% console.log(category) %>
        <% for(let i = 0; i < category.length; i++) { %>
          <tr>
            <td><%= i + 2 %> </td>
            <td><%= category[i].name %> </td>
            <td>
              <form action="/category" method="POST">
                <a href="/category/edit/<%= category[i]._id %>" class="btn btn-warning">Ubah</a>
                <a href="/category/edit/<%= category[i]._id %>" class="btn btn-danger">Hapus</a>
              </form>
            </td>
          </tr>
        <% } %> 
      </tbody>
    </table>
  </div>